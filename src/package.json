{
  "name": "lotus-explore-rs",
  "private": true,
  "scripts": {
    "dev": "dx serve --package lotus-explore-rs --platform web",
    "build:release": "dx build --package lotus-explore-rs --platform web --release --debug-symbols=false"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}
